{
  "name": "hibi",
  "version": "1.0.0",
  "repository": "git@github.com:aereal/hibi.git",
  "author": "aereal <aereal@aereal.org>",
  "license": "Unlicense",
  "private": true,
  "scripts": {
    "dev": "wsrun dev",
    "start": "NODE_ENV=production node ./server.js",
    "gcp-build": "yarn workspace front gcp-build && cp -R front/* ./ && cp -R front/.next ."
  },
  "workspaces": [
    "front",
    "admin"
  ],
  "resolutions": {
    "merge-anything": "2.4.2"
  },
  "devDependencies": {
    "wsrun": "^5.2.0"
  }
}
