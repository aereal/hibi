#import "../fragments/ListArticlesFragment.gql"

query ListArticlesQuery(
  $diaryID: ID!,
  $currentPage: Int!,
  $perPage: Int!,
  $order: ArticleOrder!
) {
  diary(id: $diaryID) {
    ...ListArticlesFragment
  }
}
